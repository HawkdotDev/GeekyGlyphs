const UserMenu = () => {
  return (
    <button id="userSection" className="bg-blue-700 h-[32px] w-[32px] rounded-full" onClick={() => alert("hem! hem!")}>

    </button>
  )
}

export default UserMenu